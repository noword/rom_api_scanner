# Usage

```
Usage: RomScanner [DB] [ROM] [-v <int>] [--help]
  DB                        database file
  ROM                       rom file for scanning
  -v, --voffset=<int>       virtual offset. If not provided, the default value
                            in the database will be used
      --help                print this help and exit
```