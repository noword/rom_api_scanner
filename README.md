# Usage

```
Usage: RomScanner [DB] [ROM] [-v <int>] [--help]
  DB                        database file
  ROM                       rom file for scanning
  -v, --voffset=<int>       virtual offset
      --help                print this help and exit
```